# Configuration moved to application.yml
fixedDelayForApigwToken.in.milliseconds=5000

# All database configurations are now managed in application.yml
# with dual-datasource structure (spring.datasource.mssql.* and spring.datasource.oracle.*)